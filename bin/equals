#!/bin/sh
# This syntax is needed for ancient shells, which have trouble with certain
# characters in the first character of a string, like - or !.
# shellcheck disable=SC2268
if test "x$1" = "x$2"
then
  exit 0
else
  exit 1
fi
